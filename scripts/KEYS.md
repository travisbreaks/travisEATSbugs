# travisEATSbugs secrets

All API keys + tokens are documented in the canonical secrets skill:
`~/code/CODE/.claude/skills/secrets.md`.

Keys live in macOS Keychain (console) + `~/.config/<provider>/<key>` files
(SSH/headless). NEVER add a literal key value to this file.
